Flask>=3.0,<4
Authlib>=1.3,<2          # OAuth2 client + JWT/JWKS verification + JWT signing
requests>=2.31,<3        # HTTP client (token exchange + discovery fetch)
python-dotenv>=1.0,<2    # .env loading
cryptography>=42,<46     # Ed25519 keypair generation (scripts/gen-rp-keypair.py)
